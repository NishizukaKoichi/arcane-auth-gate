datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model PatreonToken {
  id           Int     @id @default(1)
  accessToken  String
  refreshToken String
}
